import 'package:flutter/material.dart';

class ReferenciasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Referências'),
      ),
      body: Center(
        child: Text('Página de Referências'),
      ),
    );
  }
}
