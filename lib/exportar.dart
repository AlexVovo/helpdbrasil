import 'package:flutter/material.dart';

class ExportarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Exportar Entrevista'),
      ),
      body: Center(
        child: Text('Página de Exportar Entrevista'),
      ),
    );
  }
}
